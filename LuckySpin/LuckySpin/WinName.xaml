<Window x:Class="LuckySpin.WinName"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LuckySpin"
        mc:Ignorable="d"
        Title="WINNER" WindowStyle="None" WindowStartupLocation="CenterScreen" Loaded="Window_Loaded">
    <Grid x:Name="WinnerOverlay"
      Visibility="Collapsed"
      Panel.ZIndex="99"
      MouseDown="WinnerOverlay_MouseDown">

        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#AA6E0E0E" Offset="0"/>
                <GradientStop Color="#CC3A0A0A" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Layout đơn giản không dùng Viewbox - kích thước được tính động từ code -->
        <StackPanel HorizontalAlignment="Center"
                VerticalAlignment="Center"
                RenderTransformOrigin="0.5,0.5">

            <!-- SCALE ANIMATION -->
            <StackPanel.RenderTransform>
                <ScaleTransform x:Name="WinnerScale"
                            ScaleX="0.5"
                            ScaleY="0.5"/>
            </StackPanel.RenderTransform>

            <!-- TIÊU ĐỀ -->
            <TextBlock x:Name="TitleText"
                   Text="🎉 CHÚC MỪNG 🎉"
                   FontWeight="Bold"
                   TextAlignment="Center"
                   Margin="0,0,0,30">
                <TextBlock.Effect>
                    <DropShadowEffect Color="#3A1A00"
                      BlurRadius="8"
                      ShadowDepth="2"
                      Opacity="0.7"/>
                </TextBlock.Effect>
                <TextBlock.Foreground>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#FFF4C2" Offset="0"/>
                        <GradientStop Color="#FFD966" Offset="1"/>
                    </LinearGradientBrush>
                </TextBlock.Foreground>
            </TextBlock>

            <!-- ẢNH GIẢI THƯỞNG -->
            <Image x:Name="WinnerImage"
                   Margin="0,0,0,30"
                   Visibility="Collapsed"
                   Stretch="Uniform">
                <Image.Effect>
                    <DropShadowEffect Color="#FFD700"
                      BlurRadius="50"
                      ShadowDepth="0"
                      Opacity="0.9"/>
                </Image.Effect>
            </Image>

            <!-- TÊN NGƯỜI TRÚNG -->
            <TextBlock x:Name="WinnerNameText"
                   Text="NGUYỄN VĂN A"
                   FontWeight="ExtraBold"
                   TextAlignment="Center"
                   TextWrapping="Wrap">

                <TextBlock.Foreground>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#FFFFFF" Offset="0"/>
                        <GradientStop Color="#FFD700" Offset="1"/>
                    </LinearGradientBrush>
                </TextBlock.Foreground>

                <TextBlock.Effect>
                    <DropShadowEffect Color="#FFD700"
                  BlurRadius="40"
                  ShadowDepth="0"
                  Opacity="0.9"/>
                </TextBlock.Effect>

            </TextBlock>

        </StackPanel>
    </Grid>


</Window>
